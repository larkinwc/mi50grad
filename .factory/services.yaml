# MI50Grad Services Manifest

commands:
  install: make all
  build: make all
  test: python3 -m pytest tests/ -v
  kernels: make kernels
  hip_kernels: make hip_kernels  # Build HIP kernel shared libraries (.so) for ctypes loading
  c_extensions: make c_extensions
  clean: make clean

services: {}

# Notes:
# - All execution happens inside Docker container on dev server
# - No persistent services needed - kernels run on-demand
# - Dev server: root@192.168.1.198
# - Docker image: mixa3607/rocm-gfx906:7.1.0-complete
# - Models: /opt/models/Qwen3.5-27B-GPTQ-Int4, /opt/models/Qwen3.5-27B-AWQ
